import { Router } from 'express';

const router = Router();

// GET    /     — List all chat sessions    (Step 4)
// GET    /:id  — Get a single session       (Step 4)
// POST   /     — Save / update a session    (Step 4)
// DELETE /     — Clear all sessions         (Step 4)
// DELETE /:id  — Delete a single session    (Step 4)

export default router;
